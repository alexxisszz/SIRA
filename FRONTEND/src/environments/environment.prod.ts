export const environment = {
  production: true,
  // TODO: cuando despliegues el backend (Railway/Render/Azure), reemplaza esta URL
  // por la URL pública de tu API, por ejemplo: 'https://sira-api.up.railway.app/api'
  apiUrl: 'http://localhost:5080/api',
};
